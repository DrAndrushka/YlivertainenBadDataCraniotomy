# Ylivertainen — runtime dependencies (Python 3.12+)
# Install: pip install -r requirements.txt
# Keep this list aligned with [project.dependencies] in pyproject.toml.

# Data & numerics
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# ML / stats
scikit-learn>=1.3.0

# Plotting (DDA / EDA)
matplotlib>=3.7.0
seaborn>=0.13.0

# Notebooks (IPython.display, Markdown in modules)
ipython>=8.12.0
jinja2>=3.1.0
joblib>=1.3.0

# Optional: run Jupyter notebooks locally (uncomment if needed)
# jupyterlab>=4.0.0

# Smoke tests (unittest discover / optional pytest)
pytest>=7.4.0
